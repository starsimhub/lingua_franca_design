# CK feedback on implementation

Overall, this looks great. Specific comments are in individual files, labeled with [CK: ...].

## General comments

1. Consistently use American English spellings ("rigour" etc has slipped in).
2. I don't believe that militantly classes should be capitalized (e.g. `np.array`, `sc.dataframe`), but even I am a little confused by your choice of capitalization. why `ss.transmission()`, but `ss.Effect`? Aren't these both classes that are used to build up the model?
3. 